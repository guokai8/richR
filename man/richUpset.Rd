% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggupset.R, R/zzz_ggplots_aliases.R
\name{richUpset}
\alias{richUpset}
\alias{ggupset}
\title{UpSet plot for visualizing set intersections with color support}
\usage{
richUpset(
  x,
  sets = NULL,
  order.by = c("freq", "degree"),
  nsets = NULL,
  nintersects = 40,
  mycol = NULL,
  show.numbers = TRUE,
  number.size = 3,
  point.size = 3,
  line.size = 0.8,
  line.color = "gray23",
  color.by.set = TRUE,
  matrix.color = "black",
  inactive.color = "grey80",
  main.bar.color = NULL,
  text.scale = 1,
  set.size.show = TRUE,
  sep = ",",
  filename = NULL,
  width = 10,
  height = 6
)

ggupset(
  x,
  sets = NULL,
  order.by = c("freq", "degree"),
  nsets = NULL,
  nintersects = 40,
  mycol = NULL,
  show.numbers = TRUE,
  number.size = 3,
  point.size = 3,
  line.size = 0.8,
  line.color = "gray23",
  color.by.set = TRUE,
  matrix.color = "black",
  inactive.color = "grey80",
  main.bar.color = NULL,
  text.scale = 1,
  set.size.show = TRUE,
  sep = ",",
  filename = NULL,
  width = 10,
  height = 6
)
}
\arguments{
\item{x}{A named list of character vectors (gene sets), or a list of richResult objects}

\item{sets}{Character vector of set names to include (default: all sets)}

\item{order.by}{How to order intersections: "freq" (default) or "degree"}

\item{nsets}{Maximum number of sets to display (default: all)}

\item{nintersects}{Maximum number of intersections to display (default: 40)}

\item{mycol}{Character vector of colors for each set. If NULL, uses a default palette.}

\item{show.numbers}{Logical, show intersection size labels on bars (default: TRUE)}

\item{number.size}{Font size for intersection count labels (default: 3)}

\item{point.size}{Size of matrix dots (default: 3)}

\item{line.size}{Line width for connecting segments in the matrix (default: 0.8)}

\item{line.color}{Color for connecting lines between dots (default: "gray23")}

\item{color.by.set}{Logical, color matrix dots by set (default: TRUE). If FALSE,
active dots use a single \code{matrix.color}.}

\item{matrix.color}{Single color for active dots when \code{color.by.set = FALSE}
(default: "black")}

\item{inactive.color}{Color for inactive dots in matrix (default: "grey80")}

\item{main.bar.color}{Color for intersection bar chart. If NULL, bars use the
color of the largest set in each intersection.}

\item{text.scale}{Numeric vector of length 6 for scaling text elements:
c(intersection size title, intersection size tick labels,
set size title, set size tick labels, set names, numbers above bars).
Or a single number to scale all uniformly.}

\item{set.size.show}{Logical, show set size bar chart (default: TRUE)}

\item{sep}{Character used to separate genes in GeneID columns (default: ",")}

\item{filename}{Optional output filename for saving the plot}

\item{width}{Plot width in inches (default: 10)}

\item{height}{Plot height in inches (default: 6)}
}
\value{
A combined ggplot object (via \code{cowplot::plot_grid})
}
\description{
Creates an UpSet-style plot from named gene lists, built entirely with ggplot2.
Supports per-set coloring of bars, matrix dots, and connecting lines, without
requiring or modifying the UpSetR package.
}
\examples{
\dontrun{
# From gene lists
sets <- list(
  "Treatment A" = c("TP53", "BRCA1", "EGFR", "MYC", "PTEN"),
  "Treatment B" = c("TP53", "EGFR", "KRAS", "AKT1", "RB1"),
  "Control"     = c("TP53", "MYC", "KRAS", "CDK2", "CCND1")
)
richUpset(sets)

# Custom colors
richUpset(sets, mycol = c("dodgerblue", "goldenrod1", "seagreen3"))

# From richResult objects
res1 <- richKEGG(gene1, kodata = hsako)
res2 <- richKEGG(gene2, kodata = hsako)
richUpset(list("Sample1" = res1, "Sample2" = res2))
}
}
\author{
Junguk Hur
}
